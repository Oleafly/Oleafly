\subsection{Procedure}
Each document was converted with the engine under test and diffed against
the source. We report structural fidelity, not pixel equality. The score
combines heading recovery $h$, math recovery $m$, and table recovery $r$ as
$S = \frac{h + m + r}{3}$.
